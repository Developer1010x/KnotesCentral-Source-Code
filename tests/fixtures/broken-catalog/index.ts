// Fixture for tests/validate-data.test.ts — deliberately broken.
// `unregistered.ts` is missing from this barrel on purpose.
import { broken } from "./broken";

export const departments = [broken];
